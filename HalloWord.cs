using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HalloWord : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hallo, world!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
